# AnyBot 待办任务清单 (TODO)

本项目持久化待办任务清单，由管家王富贵维护。每当系统发现潜在问题、架构缺陷或收到优化建议时，主动登记至本清单，并在飞书交互中向老山爹请示（【同意】、【不同意】、【取消】）。

---

## 待办列表 (Pending Tasks)

### [TODO-001] 飞书群聊图片摄入与媒体暂存优化
- **任务编号**：`TODO-001`
- **登记时间**：2026-09-13 15:50
- **优先级**：高 (P1)
- **当前状态**：⏳ 待老山爹审批 (WAITING_APPROVAL)
- **问题背景**：
  在 `src/channels/feishu.ts` 中，群聊门禁检查先于图片分流处理。因为飞书纯图片无法附带 `@` 标记，导致群聊内图片被当场静默丢弃；后续文字提问无法关联图片上下文。
- **拟定方案**：
  1. **摄入解耦**：将图片与文件的本地下载落盘前置到群聊门禁判断之前，实现无论是否 `@` 均静默落盘；
  2. **Per-Chat 媒体暂存池**：按 `chatId` 维护最近 5~10 分钟内的图片本地路径，用户后续发送文字 `@王富贵` 时自动注入该暂存图片路径，实现“先发图、后提问”；
  3. **可选轻量机械回执**：支持配置项，在群内收到图片时由守护进程秒回确定性卡片/文字（不唤醒 LLM）。
- **影响范围**：`src/channels/feishu.ts`

---

## 历史归档 (Completed & Cancelled)
*(暂无已归档任务)*
